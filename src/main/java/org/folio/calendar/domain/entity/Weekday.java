package org.folio.calendar.domain.entity;

/**
 * A weekday, denoted as a lowercase string.
 *
 * Corresponds with custom type in Postgres
 */
public enum Weekday {
  sunday,
  monday,
  tuesday,
  wednesday,
  thursday,
  friday,
  saturday,
}
